# webpack-template
im sick and tired of npm install

Post-project thoughts:

I'm NOT doing project color, that field will remain unused forever... maybe one day.
Overall a pretty fun project. It took me 4 days but it was a fun 4 days.
I refactored my code a lot despite not having a viable checklist first, which took up a large chunk of the time. DOM code is really tedious to write! I really should've planned it (the DOM stuff) better, like making and using the templates and cloneNode() since I was just using createElement(). I also got stunlocked by the localStorage API since I forgot that functions get destroyed and I didn't have ways of getitng the internal lists, which I promptly added. In the end, it turned out cleaner than I expected. Except for the DOM code, which seems like an abomination.